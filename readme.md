Instructions:
-Hello
- Fork this repo
- Clone the forked repo to your local machine: `git clone <url of your fork`
- Follow the instructions at https://www.theodinproject.com/lessons/foundations-recipes
- Make at least one commit (and push) with a clear message at the end of each class you work on the project. Your commit messages should describe what you've learned and what you are struggling with

**Tips for git workflow:**
- To add all files to the staged changes: `git add .`
- To check the staged/unstaged changes: `git status`
- To commit all staged changes: `git commit`
- To push committed changes to the remote: `git push`
- To change your default editor for commit messages to your editor of choice: `git config --global core.editor "code --wait"` (or replace code with webstorm)



